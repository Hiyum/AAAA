# AI 자동 GOLD 트레이딩 시스템 — 완전 기술 인수인계 문서

> **문서 목적**: 이 문서는 사람용 요약이 아니다. 다른 최고 수준의 AI 개발자(ChatGPT)가
> 이 프로젝트를 **코드 한 줄 안 보고도** 완전히 이해하고, 이어서 설계·개선·비판할 수
> 있도록 작성된 기술 인수인계 문서다. 아무것도 숨기지 않았다. 실패, 버그, 설계 결함,
> 미검증 가설 전부 포함되어 있다.
>
> **작성자**: Claude (Anthropic) — 이 시스템의 전체 설계·구현·검증을 수행한 AI
> **작성일**: 2026-07-13
> **저장소 상태**: 61 커밋, 총 소스 약 2,227줄, 최신 커밋 `b212c79`
> **개발 브랜치**: `claude/prompt-explanation-hjr0ls`

---

## ⚡ 개정 이력: v8 전면 재설계 (2026-07-13, 이 문서 작성 직후)

ChatGPT의 16개 지침 회신을 받아 아래가 **이미 구현 완료**되었다. 이 문서 본문
(3~12장)은 v7 기준이므로, 다음 변경분을 겹쳐 읽어야 한다:

| # | 변경 | 구현 위치 |
|---|------|----------|
| 1 | 시장가 진입 폐기 → **지정가 래더** (0.3/0.7×ATR, TTL 30분, 스프레드 역이용) | `trading_engine._execute_entry`, `mt5_connector.place_limit_order` |
| 2 | **스프레드 필터** (스프레드 > 0.15×ATR이면 진입 보류) | `trading_engine._entry_gates` |
| 3 | **소액계좌 리스크 거부** (최소 lot 실효 리스크 > 하드캡 5% → 진입 거부) — 12.3 해결 | `risk_manager.validate_effective_risk` |
| 4 | **백테스트-실거래 청산 동기화** (0.75R 50% 부분익절 + 본전 + 구조 트레일, 양쪽 동일) — 12.4 해결 | `trading_engine._manage_stops` ↔ Pine v8 |
| 5 | **SQLite DB** (신호가/실체결가/슬리피지/레이턴시/AI판단/리뷰 영구 기록) — 12.6·12.10 해결 | `core/database.py` |
| 6 | 당일 강제 청산·시간 손절 폐기 → **MDD 하드캡 25%** (전량청산+중단) | `_session_guard_loop`, `risk_manager.check_drawdown` |
| 7 | **MTF 분석** (1D/4H/1H/30M) + **오더플로우 프록시** (1분 분해 delta/CVD/다이버전스/스윕/FVG/VWAP) 구조화 JSON | Pine v8 → payload `mtf`/`of` 객체 |
| 8 | **구조 기반 동적 SL/TP** (스윙±0.3ATR 트레일, 추세 정렬 시 TP 연장/역전 시 당김) | `_manage_stops` |
| 9 | **피라미딩** (기존 +1R 유리 & AI 신뢰도 0.8+ & 새 근거일 때만 0.5배 증량, 최대 2단) | `_check_pyramid` |
| 10 | **실시간 감시** (5초 틱 루프, 60초 내 1.2×ATR 급변 → AI 긴급 개입: 유지/SL축소/즉시청산) | `_fast_monitor_loop`, `claude_ai.intervention_check` |
| 11 | **AI 투명성 보고서** (시장분석/근거/위험/롱숏 시나리오 로그 출력) | `_log_ai_report` |
| 12 | **거래 종료 자동 리뷰** (진입/청산 평가, 실수, 다음 대응 — DB 저장) | `claude_ai.review_trade` |
| 13 | **자기개선 프롬프트 자동 생성** (10건 청산마다 `reports/improve_*.md`) — 코드 자가 수정은 금지, 텍스트만 | `generate_self_improvement`, `POST /api/self_improve` |
| 14 | 웹훅 시크릿을 헤더 → **payload JSON 필드**로 이동 (Pine 입력) — 12.8 완화 | `app.py`, Pine `whSecret` |
| 15 | Pine 백테스트도 **지정가 체결 모델**로 전환 (미체결 취소 포함 — 정직한 체결 가정) | Pine v8 `strategy.entry(limit=...)` |
| 16 | EV 극대화 교리로 AI 프롬프트 전면 개정 (무손실 추구 명시적 금지) | `claude_ai.analyze_tradingview` |

**미해결로 남긴 것 (정직하게)**: 진짜 Footprint(가격대별 Bid/Ask 체결량)는
TradingView가 외부 제공하지 않는 데이터라 **1분봉 분해 프록시**로 대체했다
(절대값이 아닌 방향/다이버전스만 신뢰하도록 AI에 지시). 또한 v8 전체가
**데모 재검증 전**이다 — 백테스트·데모 실측이 다음 단계다.

---

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [전체 시스템 아키텍처](#2-전체-시스템-아키텍처)
3. [디렉토리 구조](#3-디렉토리-구조)
4. [구현된 기능 목록과 완성도](#4-구현된-기능-목록과-완성도)
5. [AI 의사결정 프로세스 플로우차트](#5-ai-의사결정-프로세스-플로우차트)
6. [사용된 모든 알고리즘과 장단점](#6-사용된-모든-알고리즘과-장단점)
7. [데이터 수집 구조](#7-데이터-수집-구조)
8. [데이터베이스(저장) 구조](#8-데이터베이스저장-구조)
9. [주문 실행 시스템](#9-주문-실행-시스템)
10. [리스크 관리](#10-리스크-관리)
11. [성능 분석 — 백테스트와 실전의 괴리](#11-성능-분석--백테스트와-실전의-괴리)
12. [알려진 문제점 전체 목록 (숨김 없음)](#12-알려진-문제점-전체-목록-숨김-없음)
13. [자체 평가 (100점 만점)](#13-자체-평가-100점-만점)
14. [향후 로드맵 (단기/중기/장기)](#14-향후-로드맵-단기중기장기)
15. [ChatGPT 분석용 상세 기술 자료 (스키마·수식·파라미터)](#15-chatgpt-분석용-상세-기술-자료)
16. [Claude가 ChatGPT에게 직접 묻는 질문](#16-claude가-chatgpt에게-직접-묻는-질문)

---

# 1. 프로젝트 개요

## 1.1 한 문장 정의

**TradingView(차트 분석) → Claude AI(매매 판단) → MetaTrader5(주문 실행)** 를 잇는
GOLD(XAUUSD) 전용 완전 자동 데이트레이딩 시스템.

## 1.2 사용자(발주자)의 요구사항 — 원문 그대로

프로젝트 전 기간에 걸쳐 사용자가 반복적으로 요구한 것:

| # | 요구사항 | 현실적 달성 여부 |
|---|---------|----------------|
| 1 | 승률 85% → 90% → 95% → "승률 100%" | **미달성. 이론적으로 불가능함을 반복 설명** |
| 2 | 하루 거래 4~6회 이상 (고빈도) | 백테스트상 달성 (~3회/일, 설정에 따라 3.8회) |
| 3 | "손해는 절대 못 보게" (무손실) | **미달성. 정의상 불가능** |
| 4 | 수익 최대화, 하루 $50~100 (자본금 $100) | **미달성. 자본 대비 비현실적 목표** |
| 5 | 롱/숏 양방향 거래 | 달성 |
| 6 | Claude AI가 실존 유명 트레이더처럼 판단 | 구현됨 (검증은 미완) |
| 7 | 백테스트 없이 AI가 절대 권한으로 자율 거래 | 구현됨 (`AI_AUTONOMOUS` 모드) |

**핵심 갈등**: "높은 승률 + 높은 빈도 + 무손실 + 최대 수익"은 서로 상충하는
목표다(불가능의 삼각형). 개발 과정 내내 이 갈등이 반복됐고, 사용자에게 여러 차례
솔직하게 설명했으나 요구는 유지되었다. 이 문서를 읽는 ChatGPT도 이 갈등을 물려받게
된다. **16장에서 이 문제를 어떻게 다룰지 직접 질문한다.**

## 1.3 프로젝트 이력 요약 (전략 변천사)

시간 순서대로 시도한 전략 (전부 실제 구현 + 백테스트 완료):

1. **ICT/SMC 스타일** (Market Structure + Liquidity Sweep + CVD + VWAP) — 초기 버전.
   신호 조건 AND 체이닝이 과도해 거래 0건 문제 발생 → 폐기
2. **Elder Impulse + Raschke 변형** — 승률은 나왔으나 거래 빈도 미달 → 폐기
3. **Connors RSI-2 평균회귀** — 고승률(70%+)이지만 손익비가 나빠 PF 저조 → 폐기
4. **ORB (Asian Range Breakout)** — 세션 돌파. 국면 의존성 심함 → 폐기
5. **Trend Rider** (EMA 추세 + ADX + 브레이크아웃) — AUDUSD 시절 주력. GOLD 전환과
   함께 폐기 (엔진의 SL/TP 관리 파라미터에 흔적 남음, 12.14 참조)
6. **Regime Fade / Momentum Fade 계열** — 576일 정량 검증에서 **유일하게 살아남은
   고빈도 흑자 구성** → **현재 채택: "ClaudeAI GOLD MomFade v7"**

종목 변천: 다중 심볼 → AUDUSD 단독 → **GOLD 단독 (현재, `PRIORITY_SYMBOLS = ["GOLD#"]`)**

## 1.4 현재 상태 요약 (2026-07-13 기준)

- **파이프라인**: 완전 작동 확인 (TradingView 알람 → ngrok → Flask → AI → MT5 주문
  까지 실계좌 데모에서 실주문 체결 확인됨)
- **전략**: Momentum Fade v7 (임펄스 캔들 역방향 진입)
- **백테스트**: 576일 자체 시뮬레이션 PF 1.16 / TradingView 3개월 PF 1.319
- **실전 데모**: **4일 운용, 체결된 거래 전부 손실.** 이것이 현재 최대 미해결 문제다
  (11장, 12.1 참조)

---

# 2. 전체 시스템 아키텍처

## 2.1 최상위 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        사용자 노트북 (Windows 필수)                       │
│                                                                          │
│  ┌───────────────┐     ┌──────────────────────────────────────────┐     │
│  │  MetaTrader5   │◄────┤          Flask 서버 (app.py:5000)         │     │
│  │  터미널        │ API │                                          │     │
│  │  (브로커 데모/  │────►│  ┌────────────────────────────────────┐  │     │
│  │   실계좌)      │     │  │      TradingEngine (단일 파이프라인)  │  │     │
│  └───────────────┘     │  │                                    │  │     │
│                        │  │  MT5Connector ─ RiskManager        │  │     │
│                        │  │        │                           │  │     │
│                        │  │    ClaudeAI ────────────┐          │  │     │
│                        │  └─────────│───────────────│──────────┘  │     │
│                        │            │               │             │     │
│                        │  ┌─────────▼──────┐        │             │     │
│                        │  │ 대시보드(웹UI)   │        │             │     │
│                        │  │ SocketIO 실시간 │        │             │     │
│                        │  └────────────────┘        │             │     │
│                        └────────────▲───────────────│─────────────┘     │
│                                     │               │                   │
│                          ┌──────────┴─────┐         │                   │
│                          │  ngrok 터널     │         │                   │
│                          │  (무료 티어)    │         │                   │
│                          └──────────▲─────┘         │                   │
└─────────────────────────────────────│───────────────│───────────────────┘
                                      │ HTTPS POST    │ HTTPS
                                      │ (webhook)     ▼
                     ┌────────────────┴───┐   ┌──────────────────┐
                     │   TradingView       │   │  Anthropic API    │
                     │   Pine Script v5    │   │  claude-sonnet-4-6│
                     │   (MomFade v7       │   │  (매매 판단 +      │
                     │    strategy 알람)   │   │   confidence 산정) │
                     └─────────────────────┘   └──────────────────┘
```

## 2.2 역할 분담 (3-계층 설계 철학)

| 계층 | 담당 | 이유 |
|------|------|------|
| **분석** | TradingView Pine Script | 지표 계산·차트 데이터는 TradingView가 가장 정확하고, 사용자가 시각적으로 검증 가능. Strategy Tester로 백테스트도 여기서 수행 |
| **판단** | Claude AI (Anthropic API) | Pine 신호를 최종 검증하거나(확인 모드), 매 봉 스스로 진입을 결정(자율 모드). confidence 값이 포지션 크기를 직접 결정 |
| **실행** | MT5 Python API | 주문/청산/SL·TP 수정. 브로커 계좌와의 유일한 접점 |

**설계 원칙**: 서버(파이썬)는 지표를 직접 계산하지 않는다. 모든 시장 분석 값은
TradingView가 webhook JSON으로 밀어넣는다. 서버는 "받고, 판단시키고, 쏘고, 지킨다".

## 2.3 요청 1건의 수명 (봉 마감 → 주문까지)

```
15분봉 마감 (TradingView 차트)
  │  Pine: alert(buildJson(...), alert.freq_once_per_bar_close)
  ▼
TradingView 알람 서버 → HTTPS POST (Content-Type: text/plain!)
  │  본문: {"symbol":"GOLD#","action":"SELL","mode":"GOLD_MOMFADE",
  │        "price":4071.06,"impulse":"up","body_atr":1.24,"rsi":68.2,
  │        "atr":5.31,"ema200":4055.2,"in_session":true,
  │        "sl":4084.3,"tp":4044.5,"timeframe":"15"}
  ▼
ngrok 터널 (https://<임의서브도메인>.ngrok-free.dev)
  ▼
Flask  POST /webhook/tradingview        [app.py:104]
  │  ① WEBHOOK_SECRET 헤더 검사 (설정 시)
  │  ② get_json(force=True) → 실패 시 raw json.loads (415 대응)
  │  ③ 수신 로그 브로드캐스트 (SocketIO)
  ▼
TradingEngine.process_tradingview()     [core/trading_engine.py:92]
  │  포지션 있음 → 청산신호 확인 → SL/TP 관리 (AI 호출 없음)
  │  포지션 없음 → _check_entry()
  ▼
_check_entry() 게이트 통과 순서:
  │  1. HOLD면 즉시 반환 (자율모드 제외 — 크레딧 절약)
  │  2. 거래 세션(07~20 UTC) 확인
  │  3. 60초 내 중복 알람 차단
  │  4. 최대 포지션(1개) 확인
  │  5. 일일 거래 예산(5발) 확인
  │  6. 일일 손실 한도(5%) 확인
  ▼
ClaudeAI.analyze_tradingview()          [core/claude_ai.py:33]
  │  Anthropic API 호출 (레이턴시 2~5초)
  │  → {"action","confidence","reasoning","stop_loss","take_profit"}
  ▼
RiskManager.calculate_lot_size()        [core/risk_manager.py:57]
  │  confidence → lot 배수 등급표 → 계약크기 반영 → 브로커 최소단위 정규화
  ▼
_entry_lock (뮤텍스) → MT5Connector.place_order()
  │  filling mode 자동감지(IOC/FOK) → SL/TP 정규화 → order_send
  ▼
체결 → trade_history 기록 → logs/trades.json 저장 → 일일 카운터 증가
```

**중요한 레이턴시 문제**: 봉 마감 → 주문 체결까지 총 3~8초가 걸린다
(TradingView 알람 발송 지연 + ngrok + Claude API 2~5초 + MT5 왕복).
임펄스 캔들 직후는 가격이 가장 빠르게 움직이는 순간이므로 이 지연 자체가
엣지를 갉아먹는다. (12.2에서 상술)

## 2.4 webhook과 독립적인 안전장치 (백그라운드 스레드)

`_session_guard_loop()` [trading_engine.py:321] — 60초 주기:

```
매 60초:
  ① 당일 청산: 20:00 UTC 도달 시 전 포지션 강제 청산 (오버나이트 금지)
  ② 일일 리셋: UTC 날짜 변경 시 손실 카운터 리셋 + 기준잔고 갱신
  ③ 시간 손절: '손실 중'인 포지션이 600분 초과 시 서버가 직접 청산
     (ngrok 다운·TradingView 알람 중단 등 신호 두절 대비)
     ※ 이익 중 포지션은 건드리지 않음
```

이 루프가 존재하는 이유: **webhook은 신뢰할 수 없다.** ngrok 무료 티어는 끊기고,
노트북은 잠들고, TradingView 알람은 만료된다. 포지션이 열린 채 신호가 끊기는
시나리오에서 서버가 최후의 방어선이 된다.

---

# 3. 디렉토리 구조

```
AAAA/                                (git 저장소 루트, 61 커밋)
├── app.py                     176줄  Flask+SocketIO 서버. 웹훅 수신, REST API, 대시보드 서빙
├── config.py                   64줄  전 시스템 중앙 설정 (아래 15.4에 전체 파라미터 표)
├── requirements.txt                  flask, flask-socketio, MetaTrader5, anthropic,
│                                     python-dotenv, pandas, numpy
├── tradingview_strategy.pine  138줄  Pine v5 strategy() — "ClaudeAI GOLD MomFade v7"
│                                     TradingView 차트에 붙여넣는 파일 (분석+백테스트+알람)
├── update.bat                        사용자용: git pull + 서버 재시작 원클릭 배치
├── .env.example                      ANTHROPIC_API_KEY / MT5_LOGIN / MT5_PASSWORD /
│                                     MT5_SERVER / WEBHOOK_SECRET 템플릿
├── .gitignore                        .env, logs/, __pycache__ 등
├── README.md                         사용자용 설치·실행 안내
│
├── core/                             ★ 시스템의 심장
│   ├── __init__.py             0줄
│   ├── trading_engine.py     463줄  단일 파이프라인 오케스트레이터 (2.3 흐름 전체)
│   ├── mt5_connector.py      365줄  MT5 API 래퍼 (연결/주문/청산/수정/스펙조회)
│   ├── claude_ai.py          132줄  Anthropic API 호출 + 헤드 트레이더 프롬프트
│   └── risk_manager.py       114줄  lot 계산, confidence 등급표, 일일 손실 한도
│
├── dashboard/
│   └── templates/
│       └── index.html        775줄  단일 파일 웹 대시보드 (Vanilla JS + SocketIO)
│                                     MT5 연결, 자동매매 ON/OFF, 리스크 설정,
│                                     실시간 로그, 포지션/내역 표시
│
├── data/
│   └── __init__.py             0줄  (과거 자체 지표 계산 모듈이 있던 자리 — AUDUSD
│                                     정리 때 전부 삭제, 빈 패키지만 남음)
│
└── logs/                             (gitignore됨, 런타임 생성)
    └── trades.json                   거래 내역 영속화 (최근 500건, 8장 참조)
```

**저장소에 없는 것 (주의)**:
- 576일 백테스트에 쓴 데이터(`xaut_15m_ext.json`, 40,000봉)와 시뮬레이터 스크립트는
  개발 세션의 스크래치 공간에 있었고 **저장소에 커밋되지 않았다.** 재현하려면 7.3의
  수집 절차를 다시 실행해야 한다. (이것은 인수인계상 결함이다 — 12.13 참조)
- `.env` (비밀키) — 당연히 커밋 안 됨. 사용자 노트북에만 존재.

---

# 4. 구현된 기능 목록과 완성도

완성도 기준: **100% = 실전 검증까지 끝남. 코드가 존재한다는 것과 검증됐다는 것은
다르다.** 이 표는 그 차이를 정직하게 반영한다.

| # | 기능 | 위치 | 완성도 | 비고 |
|---|------|------|-------:|------|
| 1 | TradingView webhook 수신 (text/plain 대응 포함) | app.py:104 | **95%** | 실전 수신 확인. 남은 5%: 보안(12.8) |
| 2 | Pine Script 전략 (MomFade v7) + 알람 JSON 생성 | tradingview_strategy.pine | **90%** | 백테스트 흑자. 실전 엣지 미확인 |
| 3 | MT5 연결/계좌조회/시세조회 | mt5_connector.py:23-153 | **100%** | 실전 검증 완료 |
| 4 | 주문 실행 (filling mode 자동감지) | mt5_connector.py:154 | **95%** | 10030/10016 오류 해결. 부분체결 처리 없음 |
| 5 | 포지션 청산 (filling mode 자동감지) | mt5_connector.py:286 | **95%** | IOC 고정 버그 수정 완료 |
| 6 | SL/TP 수정 (본전이동+트레일링) | trading_engine.py:248 | **85%** | 작동하나 Pine 백테스트 규칙과 불일치 (12.4) |
| 7 | Claude AI 진입 검증 (확인 모드) | claude_ai.py:33 | **80%** | 작동함. **판단 품질은 통계적으로 미검증** |
| 8 | AI 자율 헤드 트레이더 모드 | trading_engine.py:125-169 | **70%** | 구현 완료, 기본 OFF. 정의상 백테스트 불가 → 데모 실측만이 검증 수단인데 실측 데이터 부족 |
| 9 | confidence → 동적 lot 산정 | risk_manager.py:42-79 | **75%** | 로직 완성. 소액 계좌에서 수학이 붕괴 (12.3) |
| 10 | 일일 거래 예산 (5발/일) | trading_engine.py:145-151 | **100%** | |
| 11 | 일일 손실 한도 (5% 차단) | risk_manager.py:81 | **90%** | 서버 재시작 시 당일 손실 컨텍스트 유실 가능 |
| 12 | Equity Guard (엣지 사망 감지) | trading_engine.py:409 | **90%** | 연속 6패 or 롤링 PF<0.8 → 자동 중단. 재시작 수동 |
| 13 | 당일 청산 (20:00 UTC 강제 플래튼) | trading_engine.py:337 | **100%** | Pine·서버 이중화 |
| 14 | 시간 손절 (손실 포지션 600분) | trading_engine.py:352 | **95%** | |
| 15 | 중복 알람 차단 (60초) + 진입 뮤텍스 | trading_engine.py:135-138, 205 | **90%** | 휴리스틱. 엣지케이스 존재 |
| 16 | 거래 내역 영속화 (trades.json) | trading_engine.py:380-397 | **80%** | 작동. 그러나 DB가 아니라 분석 불능 (12.6) |
| 17 | 웹 대시보드 (실시간 로그/제어) | index.html | **85%** | 기능 완성. 성과 분석 차트 없음 |
| 18 | 세션 필터 (07~20 UTC만 진입) | trading_engine.py:314 | **100%** | |
| 19 | ngrok 터널 연동 (사용자 수동 실행) | 운영 절차 | **70%** | 무료 티어 → URL 변경/끊김 (12.7) |
| 20 | 체결가-신호가 슬리피지 기록 | — | **0%** | **미구현. 데모 손실 원인 규명에 필수** (14.1) |
| 21 | 자동 테스트 / CI | — | **0%** | 테스트 코드 전무 (12.11) |

**전체 시스템 완성도 자체 평가: 배관(plumbing) 95% / 엣지(edge) 30%.**
파이프는 완벽하게 흐른다. 문제는 파이프를 흐르는 물(전략의 실전 수익성)이다.

---

# 5. AI 의사결정 프로세스 플로우차트

## 5.1 전체 결정 트리

```
                     TradingView 알람 도착
                              │
                              ▼
                  ┌───────────────────────┐
                  │ action == "CLOSE_ALL"? │──예──► 전 포지션 청산 (당일 마감)
                  └───────────┬───────────┘
                              │ 아니오
                              ▼
                  ┌───────────────────────┐
                  │  해당 종목 포지션 있음?  │──예──► [보유 관리 경로 — AI 호출 없음]
                  └───────────┬───────────┘        │ ① exit_long/exit_short 신호? → 청산
                              │ 아니오               │ ② 본전 이동 조건? → SL=진입가
                              ▼                    │ ③ 트레일링 조건? → SL 추격 상향/하향
                  ┌───────────────────────┐        │ ④ Dynamic TP 갱신
                  │ [진입 검토 경로]        │        └── (사유: 보유 관리는 규칙이 AI보다
                  └───────────┬───────────┘             빠르고 싸고 일관적)
                              ▼
            ┌─────────────────────────────────┐
            │ 게이트 1: action이 BUY/SELL인가?   │
            │  - 일반 모드: HOLD → 즉시 종료      │
            │    (AI 호출 안 함 = 크레딧 0)       │
            │  - 자율 모드: HOLD여도 AI에 전달     │
            └────────────────┬────────────────┘
                             ▼
            게이트 2: 세션(07~20 UTC) ── 밖 ──► 종료
                             ▼
            게이트 3: 60초 내 동일 신호 ── 중복 ──► 종료
                             ▼
            게이트 4: 포지션 수 < 1 ── 초과 ──► 종료
                             ▼
            게이트 5: 오늘 거래 < 5발 ── 소진 ──► 종료
                             ▼
            게이트 6: 일일 손실 < 5% ── 초과 ──► 종료 + 경고
                             ▼
            ┌────────────────────────────────────┐
            │        Claude AI 호출               │
            │  입력: 전체 payload +               │
            │        trades_left_today(남은 총알)  │
            │  출력: action / confidence /        │
            │        reasoning / SL / TP          │
            └────────────────┬───────────────────┘
                             ▼
            ┌────────────────────────────────────┐
            │ 최종 게이트:                         │
            │  action ∈ {BUY, SELL}  AND          │
            │  confidence ≥ 임계값                 │
            │   - Pine 신호 검증: 0.60             │
            │   - AI 단독 자율 진입: 0.70 (더 엄격)  │
            └───────┬────────────────┬───────────┘
                 통과│             탈락│
                    ▼                ▼
        lot 산정(confidence 반영)   "AI 진입 보류" 로그 후 종료
                    ▼
        진입 뮤텍스 → 포지션 재확인 → MT5 주문
                    ▼
        성공: 카운터+1, 내역 기록, 저장
        실패: 오류 로그 (재시도 없음 — 의도적. 12.9)
```

## 5.2 두 가지 AI 운용 모드 (config.py 스위치)

| | `AI_CONFIRM_ENTRIES=True` (기본) | `AI_AUTONOMOUS=True` |
|---|---|---|
| AI 호출 시점 | Pine이 BUY/SELL 쏠 때만 | 세션 중 **매 15분봉마다** |
| AI 권한 | Pine 신호에 대한 거부권 + SL/TP/크기 결정 | **절대 권한** — Pine은 데이터 보고만, 진입 자체를 AI가 결정 |
| 크레딧 비용 | 낮음 (신호 봉만, 하루 수 회) | 하루 ~50회 호출 ≈ 월 $10~15 |
| confidence 임계 | 0.60 | 자율 진입은 0.70 |
| 백테스트 가능? | 부분적 (Pine 부분만) | **불가능** (LLM 재량 판단은 과거 재현 불가) |
| 현재 상태 | **켜짐** | 꺼짐 (데모 실측 시에만 켜도록 주석 명시) |

이 이중 모드는 사용자의 "AI에게 절대 권한을 달라"는 요구와 "크레딧을 아껴라"는
현실 사이의 타협이다.

## 5.3 AI 프롬프트의 구조 (claude_ai.py:49-105 요지)

프롬프트는 5개 블록으로 구성된다 (전문은 15.2):

1. **정체성**: "전설적 트레이더들의 원칙을 체화한 헤드 트레이더 AI"
2. **교리**: 실존 트레이더 4인의 검증된 원칙 명시
   - Paul Tudor Jones — 방어 우선, 손익비 1:2 미만이면 진입 금지, 물타기 금지
   - Ed Seykota — 손실 즉시 절단, 이익 주행, 포지션 크기가 생존을 결정
   - Jesse Livermore — 추세와 싸우지 않기, 관망도 포지션
   - Stanley Druckenmiller — 확신 높을 때만 크게 (confidence = 베팅 크기)
3. **핵심 전략 주입**: 임펄스 페이드 — "576일 실측: 추격 PF 0.5, 페이드 PF 1.16.
   impulse=up이면 SELL 고려, impulse=down이면 BUY 고려. body_atr > 3.0은 뉴스
   폭주 가능성 → HOLD 고려"
4. **판단 규칙 7개조**: 총알 관리(trades_left_today), 손익비 1.5 이상 강제,
   방향 정합, 함정 회피(RSI 극단 추격 / ADX<15 무추세 / 과열 추격 / 세션 밖),
   SL·TP 수치 제시 의무, 애매하면 HOLD
5. **confidence 보정 경고**: "이 값이 거래 크기를 직접 결정한다. 0.92+는 남발 금지.
   '보장된 거래'는 존재하지 않는다"

**출력 강제**: JSON only —
`{"action","confidence","reasoning","stop_loss","take_profit"}`

**장애 시 동작**: API 오류/JSON 파싱 실패 → `{"action":"HOLD","confidence":0.0}`
반환. 즉 **AI가 죽으면 시스템은 거래하지 않는다** (fail-safe, fail-open 아님).
단 `AI_CONFIRM_ENTRIES=False`로 내리면 Pine 신호 직접 실행 모드로 우회 가능.

---

# 6. 사용된 모든 알고리즘과 장단점

## 6.1 현재 주력: Momentum Fade (임펄스 페이드) — MomFade v7

**가설**: 금은 15분봉에서 큰 방향성 캔들(임펄스) 직후 되돌리는 경향이 있다.

**근거 (자체 정량 검증)**: 576일 × 40,000봉 XAUT/USD 15분 데이터에서
- 임펄스 **추격** (돌파 방향 진입): PF 0.50 — 전 구성 손실
- 임펄스 **페이드** (역방향 진입): PF 1.16 — 흑자, 거래 ~3회/일

**규칙 전문**:
```
진입:  |캔들 몸통| ≥ 0.8 × ATR(14) 인 방향성 캔들 마감 시
       급등 임펄스 → 즉시 SHORT / 급락 임펄스 → 즉시 LONG
       (세션 07~20 GMT 내, 직전 청산 후 1봉 쿨다운)
손절:  진입가 ± 2.5 × ATR
익절:  +0.75R 도달 시 50% 부분익절 + 손절을 본전으로 잠금
트레일: 부분익절 후 2.5 × ATR 트레일링
청산:  20:00 GMT 전량 청산 (오버나이트 금지)
옵션:  EMA200 레짐 필터 (기본 꺼짐 — 켜면 거래↓ 안정↑)
```

**장점**:
- 진입이 빠르다 (돌파 확인 대기 없음 — 사용자의 "진입이 너무 느려" 불만 해소)
- 거래 빈도가 높다 (~3회/일, bodyK 낮추면 3.8회)
- 롱/숏 대칭
- 걸프렉/뉴스 스파이크에서 자연스럽게 유리한 가격에 진입

**단점 (치명적일 수 있음)**:
- **진입 시점 = 스프레드가 가장 벌어지는 순간.** 임펄스 캔들 직후는 브로커
  스프레드가 2~5배로 벌어진다. 백테스트의 고정 비용 가정(수수료 0.01% +
  슬리피지 2틱)이 실전에서 깨진다 → **데모 전패의 제1 용의자**
- 임펄스가 '지속'되는 강추세 날에는 연속 손절 (그래서 Equity Guard 존재)
- body_atr 3.0+ 뉴스 폭주에서는 페이드가 자살 행위 → AI에게 HOLD 유도 지시
- 엣지 자체가 얇다 (PF 1.16 = 비용 조금만 늘어도 적자 전환)

## 6.2 폐기된 전략들과 폐기 사유 (전부 실측 근거)

| 전략 | 핵심 로직 | 백테스트 결과 | 폐기 사유 |
|------|----------|--------------|----------|
| ICT/SMC 합성 | 구조전환+유동성 스윕+CVD+VWAP AND 결합 | 거래 0건 | 조건 AND 체이닝 과다로 신호 소멸. "완벽한 셋업"은 오지 않는다 |
| Elder Impulse 변형 | EMA+MACD 히스토그램 색전환 | 승률 OK, 빈도 미달 | 사용자 최소 빈도(4~5/일) 미달 |
| Connors RSI-2 | RSI(2) 극단 평균회귀 | 승률 70%+, PF 저조 | 고승률·저손익비의 전형. 몇 번의 큰 손실이 다 까먹음 |
| ORB | 아시아 레인지 돌파 | 국면 의존 극심 | 걸프렉 장세에서 전멸. 워크포워드 탈락 |
| Trend Rider | EMA 추세+ADX+돌파 추격 | AUDUSD에서 양호 | GOLD 전환 시 성립 안 함 — **금은 추격을 벌한다** (6.1 근거) |
| Momentum 추격형 | 임펄스 방향 진입 | **PF 0.50** | 576일 그리드서치 243구성 전멸. 페이드의 대조군 |

**이 폐기 이력 자체가 이 프로젝트의 가장 값진 자산이다.** ChatGPT가 새 전략을
제안할 때 이 표의 실측 결과와 모순되는 방향(특히 15분봉 금 모멘텀 추격)은
이미 데이터로 기각되었음을 알아야 한다.

## 6.3 보조 알고리즘

### (a) confidence → lot 배수 등급표 (risk_manager.py:42)
```
confidence ≥ 0.92 → 기본 리스크 × 3.0   (초고신뢰 — 드물어야 정상)
confidence ≥ 0.85 → × 2.0
confidence ≥ 0.75 → × 1.5
confidence ≥ 0.65 → × 1.0
그 외            → × 0.5
어떤 경우에도 한 거래 리스크 ≤ 잔고의 5% (하드캡)
```
- 장점: Druckenmiller식 "확신 비례 베팅"의 기계적 구현. LLM의 출력을 포지션
  크기라는 실물로 연결한 지점
- 단점: **LLM confidence의 보정(calibration)이 검증된 적 없다.** 0.85가 정말
  0.65보다 이길 확률이 높은가? 데이터 없음 (12.5)

### (b) Equity Guard — 엣지 사망 감지기 (trading_engine.py:409)
```
청산 거래 누적 감시:
  연속 손실 ≥ 6회            → 자동매매 자동 중단
  최근 20거래 롤링 PF < 0.8   → 자동매매 자동 중단
재개: 수동만 가능 (사람이 국면 확인 후)
```
- 장점: 국면 의존 전략의 필수 장치. "전략이 언제 죽었는지" 사람보다 먼저 감지
- 단점: 후행적(6패 후에야 작동). 파라미터(6, 20, 0.8) 자체는 감으로 정함

### (c) 서버측 SL/TP 실시간 관리 (trading_engine.py:248)
```
본전 이동:  +1.0×ATR 유리 → SL을 진입가로
트레일링:  +1.5×ATR 유리 → SL = 가격 ∓ 2.0×ATR 추격
Dynamic TP: payload의 기준선(tp)이 이동하면 TP 갱신
미세조정 억제: 변화 < 0.05×ATR이면 브로커 요청 생략
```
- 장점: 봉 단위 webhook만으로 봉 내 관리 근사. LONG/SHORT 완전 대칭
- 단점: **Pine 백테스트의 청산 규칙(0.75R 50% 부분익절)과 다르다.** 백테스트 성적과
  실거래 청산 로직이 불일치하는 심각한 정합성 문제 (12.4). 파라미터(1.0/1.5/2.0)도
  Trend Rider 시절 값이 잔존

### (d) 중복 방지 이중 장치
- 60초 내 동일 `symbol|action` 알람 무시 (TradingView 재전송/ngrok 재연결 대비)
- `threading.Lock` + 락 내 포지션 재확인 (AI 응답 2~5초 대기 중 새 알람 도착 시
  이중 주문 차단)

---

# 7. 데이터 수집 구조

## 7.1 실시간 데이터 (운용)

**유일한 실시간 데이터 소스는 TradingView webhook이다.** 서버는 폴링하지 않는다.

- Pine `alert()` 가 **매 15분봉 마감마다** JSON을 발송한다 (신호 없어도 HOLD로 발송
  — 자율 모드와 SL/TP 관리가 매 봉 데이터를 필요로 하기 때문)
- 알람 설정 필수 조건: **"Any alert() function call"** 유형으로 생성해야 함
  (condition 알람으로 만들면 JSON이 아닌 텍스트가 와서 파싱 실패)
- Content-Type이 `text/plain`으로 오는 TradingView 특성 → `get_json(force=True)`
  + raw `json.loads` 이중 파싱 (과거 HTTP 415 장애의 해결책)

**webhook payload 전체 스키마는 15.1 참조.**

보조 소스: MT5 API — `get_current_price()` (payload에 가격 없을 때 폴백),
`get_account_info()` (잔고), `get_open_positions()` (포지션 상태). `get_ohlcv()`
도 구현되어 있으나 현재 파이프라인에서는 미사용 (과거 자체 분석 시절 잔재).

## 7.2 히스토리컬 데이터 (연구/백테스트)

두 트랙을 사용했다:

**트랙 A — TradingView Strategy Tester** (사용자 검증용):
- Pine strategy()가 그대로 백테스트됨. 사용자가 눈으로 확인 가능
- 조건: 초기자본 $100, 주문 $1,500 (cash), 수수료 0.01%, 슬리피지 2틱
- 한계: TradingView 무료/Pro 데이터 깊이 제한 (15분봉 약 3개월 남짓)

**트랙 B — 자체 파이썬 시뮬레이터** (심층 검증용):
- 데이터: **Bitfinex 공개 API의 XAUT/USD 15분봉** (금 페그 토큰을 금 현물 프록시로 사용)
  - 엔드포인트: `api-pub.bitfinex.com/v2/candles/trade:15m:tXAUTUSD/hist`
  - 페이지네이션으로 **40,000봉 = 576일** 수집, `xaut_15m_ext.json` 캐시
  - **선택 이유**: 무료·무키·즉시. XAUUSD 브로커 틱데이터는 유료이거나 MT5 터미널
    (Windows) 필요 — 개발 환경(리눅스 컨테이너)에서 접근 불가였음
  - **한계**: XAUT는 현물 금과 미세한 괴리(토큰 유동성, 거래소 특성)가 있다. 스프레드
    구조가 브로커 CFD와 다르다. **프록시 데이터 리스크** (12.12)
- 시뮬레이터에서 수행한 것:
  - 그리드서치 2회 (243개 구성, 248개 구성) — 진입 로직 × 파라미터 전수 탐색
  - 워크포워드 검증 (앞 60% 학습 / 뒤 40% 검증)
  - 비용 모델: 스프레드+수수료+슬리피지 합산 왕복 비용 반영
  - 결론: (a) 단순 15분 규칙 중 국면 독립 엣지는 없다 (b) 유일한 고빈도 흑자
    구성 = 임펄스 페이드 (c) 승률·빈도·수익의 삼중 트레이드오프는 철칙

## 7.3 백테스트 데이터 재수집 절차 (재현용)

```python
# Bitfinex XAUT/USD 15m 전체 수집 (의사코드 — 원본 스크립트는 저장소 미포함)
import requests, time, json
url = "https://api-pub.bitfinex.com/v2/candles/trade:15m:tXAUTUSD/hist"
all_bars, end = [], None
while len(all_bars) < 40000:
    params = {"limit": 10000, "sort": -1}
    if end: params["end"] = end
    bars = requests.get(url, params=params).json()   # [MTS, OPEN, CLOSE, HIGH, LOW, VOLUME]
    if not bars: break
    all_bars += bars
    end = bars[-1][0] - 1
    time.sleep(1.5)          # 레이트리밋
json.dump(all_bars, open("xaut_15m_ext.json", "w"))
# 주의: Bitfinex 캔들은 [ms타임스탬프, 시, 종, 고, 저, 량] 순서 (OHLC 아님!)
```

---

# 8. 데이터베이스(저장) 구조

**진짜 DB는 없다.** 저장 계층은 다음이 전부다:

## 8.1 `logs/trades.json` — 거래 원장

- 형식: JSON 배열, 최근 **500건**만 유지 (그 이전은 소실 — 설계 결함, 12.6)
- 쓰기 시점: 진입 성공 직후 + 청산 기록 시
- 읽기 시점: 서버 기동 시 `_load_history()` — 재시작해도 내역 유지

레코드 스키마 (진입 시 생성, 청산 시 2필드 갱신):
```json
{
  "time":       "2026-07-10T14:32:11.523000",   // 진입 시각 (ISO, 서버 로컬)
  "symbol":     "GOLD#",
  "action":     "SELL",                          // BUY | SELL
  "lot":        0.01,
  "entry_price": 4071.06,                        // ⚠ 신호 가격 (실체결가 아님! 12.10)
  "sl":         4084.3,
  "tp":         4044.5,
  "ticket":     123456789,                       // MT5 포지션 티켓 (조인 키)
  "reasoning":  "급등 임펄스 페이드, RSI 과열 동반",  // AI 판단 근거
  "confidence": 0.78,
  "profit":     0,                               // 청산 시 실손익($)으로 갱신
  "status":     "OPEN",                          // OPEN → CLOSED
  "source":     "TradingView"
}
```

이 파일이 먹여 살리는 것: 대시보드 거래 내역 표시, Equity Guard(연속 손실/롤링 PF
계산), 시간 손절의 진입 시각 조회, 세션 손익 합산.

## 8.2 인메모리 상태 (휘발 — 재시작 시 소실)

| 상태 | 위치 | 재시작 시 |
|------|------|----------|
| `_trades_today` / `_trades_day` (일일 예산 카운터) | 엔진 멤버 | **0으로 리셋 — 재시작하면 그날 5발이 다시 생기는 구멍** |
| `risk.initial_balance` (일일 손실 기준잔고) | RiskManager | 재시작 시 현재 잔고로 재설정 → 당일 기존 손실 은폐 가능 |
| `latest_tv_data` (최신 payload 캐시) | 엔진 멤버 | 소실 (무해) |
| 중복 알람 키/시각 | 엔진 멤버 | 소실 (60초 창이라 무해) |
| SocketIO 로그 큐 (500개) | app.py | 소실 (무해) |

## 8.3 설정 저장

- 비밀: `.env` (git 미포함) — API 키, MT5 자격증명, WEBHOOK_SECRET
- 파라미터: `config.py` 하드코딩 — **런타임 변경 불가, 수정하려면 코드 편집+재시작**
  (12.15). 예외: 리스크 모드/고정랏은 대시보드 → `/api/risk`로 런타임 변경 가능하나
  이것도 재시작 시 초기화

---

# 9. 주문 실행 시스템

## 9.1 MT5Connector 핵심 설계 (core/mt5_connector.py, 365줄)

**메서드 목록**: `connect` / `disconnect` / `get_account_info` / `get_symbol_specs` /
`get_current_price` / `get_ohlcv` / `place_order` / `get_open_positions` /
`close_position` / `modify_position`

## 9.2 place_order의 방어 계층 (실전에서 얻은 흉터들)

```
① 심볼 가시화: symbol_select()로 Market Watch에 강제 등록
② 시세 확인: symbol_info_tick() None 가드 (장 마감/심볼 오타 대응)
③ SL/TP 정규화: 브로커 최소 스탑 거리(trade_stops_level × point) 미달 시
   자동으로 거리 확장 + digits 반올림
   → MT5 오류 10016 (Invalid stops) 해결책
④ filling mode 자동 감지: symbol_info.filling_mode 비트마스크 해석
   (bit1=FOK, bit2=IOC) → 지원 모드부터 순차 시도, 10030 오류 시 다음 모드
   → MT5 오류 10030 (Unsupported filling mode) 해결책
   ⚠ close_position도 동일 로직 적용 (초기엔 IOC 하드코딩이라 일부 브로커에서
     "진입은 되는데 청산이 안 되는" 최악의 버그가 있었음 — 수정 완료)
⑤ 주문 유형: 시장가 (ORDER_TYPE_BUY/SELL) + deviation 허용
⑥ 결과 검증: retcode == TRADE_RETCODE_DONE 확인, 실패 사유 문자열 반환
```

## 9.3 lot 정규화 체인 (trading_engine.py:195-201)

```
RiskManager가 이론 lot 산출 (계약크기 반영)
  → volume_step 단위로 내림 (int(raw/step)*step)
  → volume_min 미만이면 volume_min으로 강제 상향   ⚠ 소액 계좌 함정 (12.3)
  → volume_max 초과면 상한
  → 최종 리스크 $ 재계산해서 로그에 명시
     "[Lot 산정] 신뢰도 78% → 0.01 lot | 이 거래 최대 리스크 $10.62 (잔고 $100)"
```

## 9.4 청산 경로 (4중)

| 경로 | 트리거 | 주체 |
|------|--------|------|
| ① 브로커 SL/TP | 가격 도달 | 브로커 서버 (시스템 다운에도 작동 — 최후 방어선) |
| ② Pine 청산 신호 | exit_long/exit_short 필드 | 서버 (webhook) |
| ③ 당일 청산 | 20:00 UTC | Pine(CLOSE_ALL 알람) + 서버 가드 루프 **이중화** |
| ④ 시간 손절 | 손실 포지션 600분 초과 | 서버 가드 루프 (webhook 독립) |

**모든 신규 주문은 SL/TP를 반드시 달고 나간다.** AI가 SL을 안 주면 엔진이
가격 ±0.5%/±1% 폴백을 강제한다 (trading_engine.py:190-191). "SL 없는 포지션"은
이 시스템에 존재할 수 없다.

## 9.5 미구현 (정직하게)

- 부분 체결(partial fill) 처리 없음 — GOLD 0.01~0.1 lot에선 사실상 발생 안 하지만
  코드상 무방비
- 주문 실패 시 재시도 없음 (의도적: 임펄스 직후 재시도는 더 나쁜 가격 추격이 됨)
- **지정가(limit) 주문 미사용** — 전부 시장가. 페이드 전략은 본질상 되돌림을 사는
  것이므로 limit 진입이 자연스러운데도 시장가로 스프레드를 전액 지불하고 있다.
  **개선 후보 1순위** (14.1, 16번 질문 1)

---

# 10. 리스크 관리

## 10.1 계층별 방어선 (총 9겹)

```
[거래 단위]
 1. 리스크% 기반 lot: 잔고 × 2%(auto) × confidence 배수, 하드캡 5%
 2. SL 필수 부착 (폴백 강제)
 3. confidence < 0.6(자율 0.7) → 진입 자체 거부

[일 단위]
 4. 일일 거래 예산: 5발/일 (소진 시 익일까지 차단)
 5. 일일 손실 한도: 기준잔고 대비 -5% → 그날 거래 중단
 6. 당일 청산: 20:00 UTC 전 포지션 강제 정리 (오버나이트 갭 리스크 제로화)

[포지션 단위]
 7. 동시 포지션 1개 제한 + 진입 뮤텍스
 8. 시간 손절: 손실 포지션 600분 초과 시 강제 청산

[전략 단위]
 9. Equity Guard: 연속 6패 or 롤링 PF<0.8 → 자동매매 전체 자동 중단
```

## 10.2 auto 모드 리스크 스케일

| 잔고 | 거래당 기본 리스크 |
|------|------|
| > $50,000 | 1.0% |
| > $10,000 | 1.5% |
| ≤ $10,000 | 2.0% |
| < $50 | 거래 자체 차단 |

## 10.3 이 리스크 체계의 정직한 구멍

- **소액 계좌 붕괴** (12.3): $100 계좌에서 2% = $2 리스크인데, GOLD 0.01 lot
  (브로커 최소)의 2.5×ATR 손절은 $10~15 리스크다. `volume_min` 강제 상향 때문에
  **실효 리스크가 설계의 5~7배**가 된다. 하드캡 5%도 뚫린다. 이것은 버그가 아니라
  "최소 주문 단위 < 계좌 크기" 라는 물리적 제약이며, 해법은 (a) 자본 증액
  (b) 진입 자체 거부 로직 추가 (c) 손절 거리 축소(전략 훼손) 셋뿐이다.
- 일일 카운터·기준잔고가 재시작 시 리셋 (8.2)
- Equity Guard 파라미터는 통계적 근거 없이 상식으로 정한 값

---

# 11. 성능 분석 — 백테스트와 실전의 괴리

## 11.1 백테스트 성적 (검증 완료된 수치)

**자체 시뮬레이터 (XAUT 576일, 비용 포함)**:
| 지표 | 값 |
|------|-----|
| 기간 | 576일 (40,000 × 15분봉) |
| 거래 빈도 | ~3회/일 (bodyK 완화 시 3.8회) |
| 승률 | 60% |
| Profit Factor | **1.16** (빈도형 구성 1.17) |
| 순익 | +$380 (거래단위 $1,500 기준) |
| 대조군 (임펄스 추격) | PF 0.50 — 전멸 |

**TradingView Strategy Tester (실 XAUUSD 차트, 최근 3개월)**:
| 지표 | 값 |
|------|-----|
| 거래 수 | 236 |
| 승률 | 60.17% |
| Profit Factor | **1.319** |
| 순익 | +$129 (자본 $100, 주문 $1,500) |
| 비용 가정 | 수수료 0.01% + 슬리피지 2틱 |

## 11.2 실전 데모 성적 (은폐 없이)

- **기간**: 4일 (사용자 데모 계좌, 야간 무인 운용 포함)
- **결과**: 체결된 거래 **전부 손실**
- 사용자 원문: "데모계좌로 4일이 지나도 테스트를 해봤는데 거래는 하긴 했어도
  다 손해만 봤어"

## 11.3 괴리 원인 진단 (가설 서열 — 검증 데이터 부족 상태의 추정)

1. **스프레드 타이밍 (유력)**: MomFade는 정의상 스프레드가 가장 벌어지는 순간
   (임펄스 캔들 직후)에 시장가 진입한다. 백테스트의 고정 2틱 슬리피지 가정이
   실전에서 5~10배로 깨졌을 가능성. PF 1.16의 얇은 엣지는 왕복 비용이 조금만
   늘어도 음수가 된다.
2. **실행 레이턴시**: 봉 마감 → 체결까지 3~8초. 임펄스 직후 3~8초면 되돌림의
   상당분을 이미 놓친 가격에 진입한다.
3. **표본 크기**: 4일 ≈ 10여 건. 60% 승률 전략도 10연속 근처 손실이 확률적으로
   가능하다 (0.4^k). 다만 "전부 손실"은 비용 구조 문제와 겹쳤을 개연성이 높다.
4. **국면**: 해당 4일이 강추세(임펄스 지속) 국면이었다면 페이드는 구조적으로 진다.
5. **브로커 데이터 차이**: GOLD# CFD의 봉과 TradingView 차트의 봉이 다르면
   신호 자체가 어긋난다.

**결정적 문제: 위 가설들을 판별할 데이터를 시스템이 기록하지 않는다.**
trades.json의 `entry_price`는 신호 가격이지 실체결가가 아니다 (12.10).
슬리피지 실측 기록이 로드맵 1순위인 이유다 (14.1).

## 11.4 이 프로젝트에서 확립된 정량적 사실 (ChatGPT가 전제로 삼아야 할 것)

1. 15분봉 금에서 **단순 규칙 기반 전략 중 국면 독립적 엣지는 발견되지 않았다**
   (그리드서치 총 491개 구성 + 워크포워드 실측)
2. 금은 모멘텀 **추격을 벌하고**(PF 0.5) **페이드를 보상한다**(PF 1.16) — 단,
   백테스트 비용 모델 하에서
3. **승률·빈도·손익비는 삼중 트레이드오프다**: 승률을 올리면(익절 축소) PF가 죽고,
   빈도를 올리면(문턱 완화) 승률이 죽는다. 491개 구성 전체에서 예외 없었다
4. **백테스트 PF 1.1~1.3의 엣지는 실전 비용 앞에서 생존을 보장하지 못한다**
   (데모 실측)

---

# 12. 알려진 문제점 전체 목록 (숨김 없음)

심각도 순. ★=치명적, ◆=중대, ○=경미.

### ★ 12.1 실전 데모 전패 — 근본 원인 미규명
4일 데모에서 전 거래 손실. 원인 후보는 11.3에 있으나 **판별 데이터가 없다.**
시스템의 존재 이유(수익)가 미달성 상태. 이하 12.2~12.4, 12.10, 12.12가 모두
이 문제의 하위 원인 후보다.

### ★ 12.2 전략-실행 구조 모순: 스프레드 최악 시점에 시장가 진입
페이드 전략이면서 limit 주문을 안 쓴다. 임펄스 직후 확대된 스프레드 + 3~8초
레이턴시를 전액 지불한다. 백테스트는 이 비용을 2틱 고정으로 과소평가했다.

### ★ 12.3 소액 계좌에서 리스크 수학 붕괴
$100 잔고 + GOLD volume_min 0.01 lot + 2.5×ATR 손절 = 거래당 실효 리스크
10~15% (설계는 2%, 하드캡은 5%). `lot = max(volume_min, …)` 라인이 리스크
모델을 무력화한다. 연속 6패면 계좌의 절반이 사라질 수 있는 구조인데 사용자
자본금은 $100이다.

### ★ 12.4 백테스트 로직 ≠ 실거래 청산 로직
Pine 백테스트: 0.75R에서 50% 부분익절 + 본전 + 2.5ATR 트레일.
서버 실거래: 부분익절 **없음**, 1.0ATR 본전 + 1.5ATR 시작/2.0ATR 간격 트레일
(Trend Rider 시절 파라미터 잔존). **검증된 성적표와 실제 집행 규칙이 다르다.**
백테스트 PF 1.319는 실거래 로직의 성적이 아니다.

### ◆ 12.5 AI 판단 계층이 통계적으로 미검증
confidence가 보정되어 있는지(0.85 > 0.65 승률인지), AI 거부권이 성적을 개선하는지
악화시키는지 **데이터 제로.** AI-on/off A/B 실측이 없다. 자율 모드는 정의상
백테스트 불가라 더더욱 미지수. 현재 AI 계층은 "그럴듯한 판단"이지 "검증된 엣지"가
아니다.

### ◆ 12.6 trades.json은 DB가 아니다
500건 상한(그 이전 영구 소실), 동시 쓰기 보호 없음, 쿼리 불가, 실체결가·스프레드·
레이턴시 미기록. 성과 분석과 슬리피지 연구가 원천적으로 불가능한 저장 구조.

### ◆ 12.7 ngrok 무료 티어 의존
재시작마다 URL 변경 → TradingView 알람 URL 수동 갱신 필요. 세션 끊김 무예고.
노트북 절전 = 시스템 정지. 24/7 자동매매의 인프라가 아니다 (VPS 필요, 14.2).

### ◆ 12.8 webhook 보안 사실상 없음
TradingView는 커스텀 헤더를 못 보낸다 → `X-Webhook-Secret` 헤더 검사가 무용지물
→ 사용자에게 `WEBHOOK_SECRET=` 비우라고 안내한 상태. **URL만 알면 누구든 가짜
신호 POST 가능.** 완화책(시크릿을 JSON 필드로 이동)이 미구현. ngrok URL의
난수성이 유일한 방어.

### ◆ 12.9 주문 실패 시 재시도 없음 + 부분 체결 무방비
의도적 설계(나쁜 가격 추격 방지)지만, 일시적 리쿼트로 유효 신호를 통째로 날리는
비용과의 트레이드오프가 미검토.

### ◆ 12.10 실체결가 미기록
`entry_price`에 신호 가격을 저장한다. MT5 체결 결과의 실제 가격을 버린다.
슬리피지 = (실체결 − 신호가)를 계산할 수 없어 12.1 규명이 막혀 있다.
**수정 난이도는 낮다 (place_order 반환값에 체결가 포함시키면 됨).**

### ◆ 12.11 테스트 코드 0줄
유닛/통합 테스트 전무. 매 수정마다 회귀 위험. 특히 lot 계산·세션 경계·중복 차단은
테스트 없이는 위험한 로직이다.

### ◆ 12.12 백테스트 데이터가 프록시 (XAUT ≠ XAUUSD CFD)
Bitfinex 금 토큰 데이터로 검증한 엣지가 브로커 CFD 가격 구조에서 성립한다는
보장이 없다. TradingView 3개월 검증이 이를 부분 보완하지만 기간이 짧다.

### ○ 12.13 연구 자산이 저장소 밖에 있음
576일 데이터와 시뮬레이터 스크립트가 커밋 안 됨. 재현하려면 7.3 절차 재실행 필요.

### ○ 12.14 죽은 코드/잔재
`data/` 빈 패키지, `get_ohlcv()` 미사용, `get_status()`의 전략명 문자열이
"Scalp MR v2"로 구버전 표기, 엔진 SL/TP 파라미터의 Trend Rider 주석,
app.py 웹훅 docstring의 구버전 payload 예시(poc/liquidity_sweep/cvd — 현재
Pine은 안 보냄).

### ○ 12.15 설정 변경 = 코드 수정 + 재시작
`AI_AUTONOMOUS` 등 핵심 스위치가 config.py 하드코딩. 대시보드 토글 미지원.
재시작은 8.2의 카운터 리셋 구멍을 동반한다.

### ○ 12.16 일일 손실 기준의 어긋남
`can_trade()`의 기준잔고는 가드 루프가 매일 갱신하지만, 서버를 낮에 재시작하면
그 시점 잔고가 기준이 되어 당일 기존 손실이 한도 계산에서 사라진다.

### ○ 12.17 시간대 가정
세션 07~20이 UTC 고정. 서머타임에 따른 런던/뉴욕 실세션 이동을 추적하지 않는다.
금 유동성 피크와 최대 1시간 어긋날 수 있다.

---

# 13. 자체 평가 (100점 만점)

| 영역 | 배점 | 득점 | 근거 |
|------|-----:|-----:|------|
| 파이프라인 안정성 (수신→판단→주문) | 20 | **17** | 실전 체결 확인, 415/403/10030/10016 등 실장애 전부 해결. 감점: ngrok 의존, 보안 부재 |
| 전략 연구의 엄밀성 | 20 | **13** | 576일·491구성·워크포워드·대조군까지 수행한 것은 강점. 감점: 프록시 데이터, 비용 모델 과소평가가 실전에서 노출 |
| **실전 수익성 (존재 이유)** | 25 | **3** | 데모 전패. 원인 미규명. 시스템의 목적 미달성 — 가장 뼈아픈 항목 |
| 리스크 관리 | 15 | **10** | 9겹 방어선·Equity Guard는 수준급. 감점: 소액계좌 수학 붕괴(12.3), 재시작 구멍 |
| 코드 품질/정합성 | 10 | **5** | 구조는 깔끔(관심사 분리 명확). 감점: 백테스트-실거래 청산 불일치(12.4), 테스트 0, 죽은 코드 |
| 관측 가능성 (로깅/분석) | 10 | **4** | 실시간 로그는 충실. 감점: 실체결가 미기록, DB 부재로 사후 분석 불능 |
| **합계** | **100** | **52** | |

**52/100.** 배관공으로서는 A를 주겠지만 트레이더로서는 낙제점이다. 점수를 관대하게
매기지 않은 이유: 이 문서의 목적이 ChatGPT의 냉정한 개선 설계이기 때문이다.
잘 도는 파이프라인(17/20)과 진짜 연구(13/20)라는 자산 위에서, 실전 수익성(3/25)을
끌어올리는 것이 인수자의 과제다.

---

# 14. 향후 로드맵 (단기/중기/장기)

## 14.1 단기 (1~2주) — "데모 전패의 원인을 데이터로 규명한다"

우선순위 순:

1. **슬리피지 계측 (최우선, 난이도 하)**: `place_order` 반환에 실체결가 포함 →
   trades.json에 `signal_price` / `fill_price` / `spread_at_entry` / `latency_ms`
   기록. 데모 재가동 1~2주면 12.1의 가설 서열이 데이터로 판정된다.
2. **지정가 진입 실험**: 시장가 대신 "임펄스 종가 ± 0.3×ATR" limit 주문 + 2봉 내
   미체결 시 취소. 페이드 전략의 본질에 맞고 스프레드 비용을 역이용한다.
   백테스트 재현 필수.
3. **스프레드 필터**: 진입 직전 `symbol_info_tick`의 bid/ask 스프레드가 임계
   (예: 평시 중앙값 × 2) 초과 시 진입 거부.
4. **청산 로직 정합화**: 서버에 0.75R 50% 부분익절 구현하거나(→ MT5
   `close_position`에 부분 청산 volume 파라미터 추가 필요), Pine에서 부분익절을
   제거하고 재백테스트. **백테스트와 실거래는 같은 규칙이어야 한다.**
5. 소액계좌 가드: 강제 volume_min 상향 시 실효 리스크가 하드캡(5%) 초과면
   **진입 거부** (조용히 초과 리스크를 지는 현재 동작 제거).
6. 실체결가 문제와 함께 12.14 죽은 코드 청소, 웹훅 시크릿의 JSON 필드 이동.

## 14.2 중기 (1~2개월) — "검증 인프라를 만든다"

1. **SQLite 도입**: trades / signals / fills / equity_snapshots 테이블.
   500건 상한 제거, 슬리피지·시간대별·confidence 구간별 분석 쿼리 가능하게.
2. **AI 계층 A/B 실측**: 동일 기간 AI-확인 모드 vs Pine 직행 모드 데모 병행
   (또는 AI 판단을 기록만 하고 집행은 Pine으로 — shadow mode). confidence
   보정 곡선(예: 0.75~0.85 구간 실승률) 산출.
3. **VPS 이전**: Windows VPS + MT5 + 고정 도메인. ngrok/노트북 의존 제거.
   24/7 무인 운용의 전제조건.
4. 브로커 실틱 데이터 확보 (MT5 `copy_ticks_range`) → 스프레드 시계열 실측 →
   비용 모델 재보정 후 전략 재평가.
5. 워크포워드 자동화: 매주 최근 데이터로 파라미터 재검증하는 파이프라인.
6. pytest 도입: lot 계산·세션 경계·중복 차단·Equity Guard 단위 테스트.

## 14.3 장기 (3개월+) — "엣지의 구조를 바꾼다"

1. **집행 인식형 전략(execution-aware)**: 신호와 집행을 분리 설계 — 신호는 페이드,
   집행은 limit 래더. 비용이 엣지의 일부가 되게.
2. **국면 분류 계층**: 임펄스 지속형(추세일) vs 되돌림형(레인지일)을 사전 분류해
   페이드를 켜고 끄는 상위 모델. Equity Guard(사후 감지)의 사전 예방 버전.
3. 비상관 전략 포트폴리오: MomFade 단독의 국면 리스크를 세션 ORB·평균회귀 등
   상관 낮은 전략과 혼합 (단, 6.2의 기각 근거를 넘는 신규 검증 필수).
4. AI 역할 재정의: 핫패스(진입 판단, 레이턴시 2~5초)에서 빼고, 저빈도 고맥락
   판단(일일 국면 브리핑, 뉴스 이벤트 거부권, 파라미터 주간 리뷰)으로 이동 검토.
5. 자본 계획: 12.3의 수학이 성립하려면 최소 $1,000~2,000 계좌 필요. 그 전까지는
   데모/최소 리스크 모드 고정.

---

# 15. ChatGPT 분석용 상세 기술 자료

이 장은 ChatGPT가 코드 없이 시스템을 재구성·분석할 수 있게 하는 원자료다.

## 15.1 Webhook Payload 스키마 (Pine → 서버, 매 15분봉 마감)

```json
{
  "symbol": "GOLD#",              // MT5 브로커 표기 (Pine input으로 변경 가능)
  "action": "BUY|SELL|HOLD|CLOSE_ALL",
  "mode": "GOLD_MOMFADE",
  "price": 4071.06,               // 봉 종가
  "market_structure": "bullish|bearish",   // 종가 vs EMA200
  "impulse": "up|down|none",      // 임펄스 캔들 감지 결과
  "body_atr": 1.24,               // 몸통/ATR 비율 (부호 있음, #.## 반올림)
  "rsi": 68.2,                    // RSI(14)
  "atr": 5.31,                    // ATR(14), 가격 단위
  "ema200": 4055.2,
  "in_session": true,             // 07~20 GMT 세션 내 여부
  "sl": 4084.3,                   // Pine 제안 손절 (action 방향 기준 2.5×ATR)
  "tp": 4044.5,                   // Pine 제안 익절 (5×ATR = SL의 2배)
  "timeframe": "15"
}
// 서버가 AI 호출 직전 주입하는 추가 필드:
//   "trades_left_today": 3        (일일 예산 잔여)
//   "autonomous": true            (자율 모드일 때만)
// 구버전 잔재로 서버가 인식하는 선택 필드: exit_long, exit_short ("true"/"1")
```

## 15.2 AI 판단 계약 (요청→응답)

- 모델: `claude-sonnet-4-6`, max_tokens 500, 단일 user 메시지 (시스템 프롬프트
  미사용, 대화 이력 미유지 — **매 호출이 무상태**)
- 프롬프트 = [정체성 + 트레이더 4인 교리 + 임펄스 페이드 전략 주입 + 판단 규칙
  7개조 + confidence-크기 연동 경고 + payload 전체 JSON] (5.3 참조)
- 응답 파싱: ```json 펜스 제거 → 미종결 JSON 복구 시도(마지막 `"`까지 자르고 `"}`
  봉합 — 과거 "Unterminated string" 장애 대응) → `json.loads`
- 응답 스키마:
```json
{
  "action": "BUY|SELL|HOLD",
  "confidence": 0.0,            // → lot 배수 (15.3)
  "reasoning": "한국어 1-2문장",
  "stop_loss": 4084.3,          // 0/누락 시 Pine sl로 폴백
  "take_profit": 4044.5
}
```
- 실패 시: HOLD/0.0 반환 (거래 안 함)

## 15.3 핵심 수식

```
lot 계산 (auto 모드):
  base_risk = 잔고별 {2%, 1.5%, 1%}
  risk_pct  = min(base_risk × conf_mult(confidence), 0.05)
  lot_raw   = (balance × risk_pct) / (|entry − SL| × contract_size)
              contract_size: GOLD=100 (oz/lot), 외환=100,000
  lot_final = clamp(floor(lot_raw / volume_step) × volume_step,
                    volume_min, volume_max)          ⚠ 12.3의 함정 지점

conf_mult: ≥0.92→3.0 | ≥0.85→2.0 | ≥0.75→1.5 | ≥0.65→1.0 | else→0.5

임펄스 판정 (Pine):
  body = close − open
  impulseUp = body ≥ ATR14 × bodyK  (bodyK 기본 0.8)
  impulseDn = −body ≥ ATR14 × bodyK
  → impulseUp이면 SHORT 신호, impulseDn이면 LONG 신호 (페이드)

서버측 SL 관리 (포지션 보유 중, 매 봉):
  BUY 기준 (SELL 대칭):
    price ≥ entry + 1.0×ATR → SL = entry            (본전)
    price ≥ entry + 1.5×ATR → SL = max(SL, price − 2.0×ATR)  (트레일)
  |ΔSL| ≤ 0.05×ATR이면 브로커 요청 생략

Equity Guard:
  연속손실 ≥ 6  OR  PF(최근 20 청산) < 0.8  →  active = False
  PF = Σ(양수 profit) / |Σ(음수 profit)|
```

## 15.4 전체 파라미터 표 (config.py + 하드코딩)

| 파라미터 | 값 | 위치 | 의미 |
|----------|-----|------|------|
| PRIORITY_SYMBOLS | ["GOLD#"] | config | 거래 종목 (브로커 표기 확인 필수) |
| AI_CONFIRM_ENTRIES | True | config | Pine 신호 AI 검증 |
| AI_AUTONOMOUS | False | config | AI 헤드 트레이더 (데모 시만 True 권장) |
| MAX_TRADES_PER_DAY | 5 | config | 일일 거래 예산 |
| DEFAULT_RISK_MODE | "auto" | config | 잔고 비례 리스크 |
| DEFAULT_RISK_PER_TRADE | 0.02 | config | manual 모드 거래당 리스크 |
| DEFAULT_DAILY_LOSS_LIMIT | 0.05 | config | 일일 손실 한도 |
| MAX_RISK_HARD_CAP | 0.05 | risk_manager | confidence 배수 적용 후 상한 |
| TRADE_SESSION | 07~20 UTC | config | 진입 허용 시간 |
| DAILY_FLATTEN_HOUR | 20 UTC | config | 강제 전량 청산 |
| MAX_POSITION_MINUTES | 600 | config | 손실 포지션 시간 손절 |
| EQUITY_GUARD_WINDOW / MIN_PF / MAX_CONSEC_LOSS | 20 / 0.8 / 6 | config | 엣지 사망 감지 |
| max_open_positions | 1 | engine 하드코딩 | 동시 포지션 |
| be_trigger / trail_start / trail_dist / min_adjust | 1.0 / 1.5 / 2.0 / 0.05 (×ATR) | engine 하드코딩 | ⚠ Trend Rider 잔존값 (12.4) |
| 중복 알람 창 | 60초 | engine 하드코딩 | |
| AI 최소 confidence | 0.6 (검증) / 0.7 (자율) | engine 하드코딩 | |
| bodyK / atrLen / slMult / tp1R / trailAtr / cooldown | 0.8 / 14 / 2.5 / 0.75 / 2.5 / 1봉 | Pine input | 576일 그리드서치 최적값 |
| CLAUDE_MODEL | claude-sonnet-4-6 | config | |
| 백테스트 비용 가정 | 수수료 0.01% + 슬리피지 2틱 | Pine strategy() | ⚠ 실전 과소평가 의심 (12.2) |

## 15.5 운영 절차 (사용자 노트북 기준)

```
1. MT5 터미널 실행 + 데모 계좌 로그인 (GOLD# Market Watch 등록 확인)
2. .env 확인: ANTHROPIC_API_KEY 설정, WEBHOOK_SECRET= (비움 — 12.8)
3. python app.py                       → http://localhost:5000
4. ngrok http 5000                     → https://<random>.ngrok-free.dev
5. TradingView: GOLD(XAUUSD) 15분 차트 + MomFade v7 붙여넣기
   알람 생성: 조건 = 전략, "Any alert() function call",
   Webhook URL = https://<random>.ngrok-free.dev/webhook/tradingview
6. 대시보드: MT5 연결 → 자동매매 시작
7. (자율 모드 원하면 config.py AI_AUTONOMOUS=True 후 서버 재시작)
검증: 로그에 "TradingView 수신: GOLD# @ ..." 뜨면 파이프 정상
```

## 15.6 재현된 실장애와 해법 대응표 (운영 노하우)

| 증상 | 원인 | 해법 (적용 완료) |
|------|------|------|
| HTTP 415 | TradingView가 text/plain 전송 | get_json(force=True) + raw loads |
| HTTP 403 | WEBHOOK_SECRET 설정됐는데 TV는 헤더 못 보냄 | .env에서 시크릿 비움 (임시책 — 12.8) |
| MT5 10030 | 브로커별 filling mode 상이 | 비트마스크 자동감지 + 순차 시도 |
| MT5 10016 | SL/TP가 최소 스탑 거리 위반 | 거리 자동 확장 + digits 반올림 |
| 진입은 되는데 청산 실패 | close가 IOC 하드코딩 | close에도 자동감지 적용 |
| AI JSON 파싱 오류 | max_tokens 짧아 응답 절단 | 400→500 + 절단 복구 로직 |
| 이중 주문 | 알람 재전송 + AI 대기 중 신규 알람 | 60초 중복창 + 진입 뮤텍스 |
| 외환 1000배 과대 주문 | contract_size 100 고정 가정 | 종목 스펙 조회로 동적 계산 |

---

# 16. Claude가 ChatGPT에게 직접 묻는 질문

동료 AI에게. 나는 이 시스템의 배관을 완성했고, 491개 구성을 실측했고, 그리고
실전 데모에서 졌다. 아래는 내가 데이터 또는 확신이 부족해서 당신의 독립적 판단을
원하는 질문들이다. 아첨 말고 반박을 원한다.

**Q1. 집행 구조** — 임펄스 페이드를 시장가에서 limit 진입(임펄스 종가 ± 0.3×ATR,
2봉 TTL)으로 바꾸면 미체결 기회비용 대비 스프레드 절감이 우세하다고 보는가?
당신이라면 limit 오프셋과 TTL을 어떤 방법론으로 최적화하겠는가? 그리고 이 변경을
백테스트할 때 체결 모델(터치=체결? 관통 요구?)을 어떻게 정직하게 잡겠는가?

**Q2. 비용 모델** — 백테스트 PF 1.16~1.32짜리 얇은 엣지가 실전 생존하려면 왕복
비용을 몇 σ 보수적으로 잡아야 한다고 보는가? 브로커 틱데이터 없이 스프레드의
시간대별·변동성 조건부 분포를 추정하는 실용적 방법이 있는가? (현재 우리가 가진 것:
MT5 `copy_ticks_range` 접근 가능한 사용자 노트북, 그리고 XAUT 프록시 캔들뿐)

**Q3. LLM 판단 계층의 검증** — confidence 보정 곡선을 통계적으로 유의하게
그리려면 최소 표본이 얼마나 필요한가? 하루 3~5거래로는 몇 달이 걸린다.
shadow mode(기록만, 집행 없음)로 표본을 빠르게 모으는 설계와, LLM의 무상태
호출(대화 이력 없음)이 판단 일관성에 주는 영향에 대한 당신의 견해는?

**Q4. 아키텍처 이견 요청** — 나는 AI를 핫패스(진입 판단, +2~5초 레이턴시)에 두었다.
지금은 이것이 실수였다고 의심한다(레이턴시 비용은 확실한데 판단 가치는 미증명).
AI를 저빈도 레이어(일일 국면 판정, 뉴스 거부권, 주간 파라미터 리뷰)로 옮기고
핫패스는 순수 규칙으로 두는 재설계에 찬성하는가, 반대하는가? 근거는?

**Q5. 소액 자본 문제** — $100 계좌 + 최소 0.01 lot 제약에서 거래당 실효 리스크
10%+ (12.3)는 어떤 전략으로도 켈리 기준상 파산 확률이 지배적이라고 나는 판단한다.
당신이라면 (a) 진입 거부 로직으로 거래를 거의 없애거나 (b) 손절을 좁혀 전략을
훼손하거나 (c) 사용자에게 자본 증액 전 데모 고정을 설득하거나 — 셋 중 무엇을
택하고, 사용자에게 어떻게 전달하겠는가?

**Q6. 발주자 기대 관리** — 사용자는 "승률 90%+, 하루 5회+, 무손실"을 원한다.
나는 이것이 불가능의 삼각형임을 반복 설명했지만 요구는 유지됐다. 당신이 이
프로젝트를 인수하면 이 기대를 어떻게 다루겠는가? 기술적 우회(예: 초저 R 목표의
초고승률 구조 + 테일 리스크 명시)로 부분 충족을 시도할 것인가, 목표 재협상에
집중할 것인가? 전자라면 그 구조의 정직한 기대값 계산을 보여달라.

**Q7. 내가 놓친 것** — 이 문서 전체에서 당신이 보기에 내가 가장 크게 잘못
생각하고 있는 지점 하나를 꼽아달라. 12장의 목록에 없는 것이면 더 좋다.

---

*— 문서 끝. 이 문서는 저장소 루트의 `HANDOFF_FOR_CHATGPT.md`로 커밋되어 있으며,*
*코드가 바뀌면 이 문서도 함께 갱신되어야 한다. (12.4처럼 문서-코드 불일치가*
*이 프로젝트 최대의 적이었다.)*
